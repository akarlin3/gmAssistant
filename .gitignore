node_modules
.next
.env
.env.local
.DS_Store
*.log
*.tsbuildinfo
firebase-config.json

# next-pwa generated service worker artifacts — emitted fresh by `next build`
# (B-05). Committing them ships a stale precache manifest, so let each build
# regenerate them in public/.
public/sw.js
public/sw.js.map
public/workbox-*.js
public/workbox-*.js.map
